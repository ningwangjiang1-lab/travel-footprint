# 旅行足迹 · 开发计划 v1.0

> **依据文档**：《旅行攻略编辑器-产品文档.md》（PRD）、《UI设计规范.md》（UI Spec）、《demo.html》（视觉参考）
> **目标**：按依赖顺序拆分为可独立验证的步骤，每步可交给一个新的 AI 会话独立完成
> **更新日期**：2026-09-07

---

## 0. 如何使用这份计划

1. **每步 = 一个独立开发单元**。每个 Step 都写明了：要实现的功能、涉及的文件、以及「怎么判断做完了」。
2. **每步可开新会话**。每个 Step 自带足够的上下文（内联关键需求 + 指向 PRD/UI 规范的引用），新会话无需重读全部历史即可独立完成该步。
3. **严格按顺序**。Step 编号即依赖顺序，先基础（脚手架、数据）后功能（页面、条目），最后收尾与增强。
4. **每个 Step 结束时先自测**（对照「完成标准」），通过后再进入下一步。

**贯穿全程的三条硬约束**：
- **样式必须对齐 UI 规范**：所有颜色/字号/圆角/阴影一律引用 `tokens.css` 里的变量（§2 的 CSS 变量），禁止手写魔法值。
- **移动端优先**：单列布局 + 底部 Tab，桌面预览以「手机框」呈现（UI Spec §7.2、响应式规则）。
- **单一编辑态**：不设「规划/记录/分享」模式切换，条目随时可打卡（PRD §4 P1 自由编辑原则）。

---

## 1. 目标目录结构（每步「涉及文件」以此为准）

```
旅行足迹/
├── index.html
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
├── public/                     # 静态资源（插画、图标）
└── src/
    ├── main.tsx                # 入口
    ├── App.tsx                 # 根组件
    ├── router.tsx              # 路由
    ├── styles/
    │   ├── tokens.css          # 设计 Token（UI Spec §2，唯一配色来源）
    │   └── global.css          # 字体族、reset、全局样式
    ├── types/
    │   └── index.ts            # Guide / Day / Item / Location / Expense 类型
    ├── store/
    │   └── useStore.ts         # Zustand store + persist
    ├── lib/
    │   ├── id.ts               # 生成 id
    │   ├── format.ts           # 金额 / 日期 / 星期 / 百分比格式化
    │   ├── locations.ts        # 地点聚合统计（国家/城市/地点数）
    │   └── expense.ts          # 账单聚合统计
    ├── components/
    │   ├── layout/             # StatusBar / AppBar / TabBar / FAB
    │   ├── ui/                 # Button / KpiCard / Card / Chip / Badge / Progress / EmptyState / BottomSheet / Loading
    │   ├── guide/              # GuideCard / DayBlock / Item / ItemIcon / QuickAddBar / NewGuideModal / ShareImage
    │   ├── map/                # MapView / MapPin / MapStats / CityChips
    │   └── bill/               # CategoryDonut / TrendChart / BillRow / Filters
    └── pages/
        ├── HomePage.tsx
        ├── MapPage.tsx
        ├── BillPage.tsx
        └── EditorPage.tsx
```

---

## 2. 开发步骤

### Step 0 · 项目初始化与设计 Token 落地

**依赖**：无

**目标**：跑通工程，把 UI 规范 §2 的全部设计 Token 落地为 CSS 变量与 Tailwind 主题，建立全局骨架。

**要实现的功能**
1. 用 Vite 创建 `React + TypeScript` 项目。
2. 安装依赖（见 §3 技术栈总览）。
3. 创建 `src/styles/tokens.css`：完整复制 UI 规范 §2 的 `:root` 与 `body.dark` 变量块（品牌色、中性色、类别色、圆角、阴影）。
4. 创建 `src/styles/global.css`：字体族（UI Spec §4.1）、reset、`body` 背景 `var(--cream)` 文字 `var(--ink)`、颜色/背景过渡 0.35s。
5. 配置 `tailwind.config.js`：把 Token 映射进 Tailwind theme —— `colors`（brown/cream/olive/orange/night/ink/ink-2/ink-3/card/line/c-*）、`fontSize`（§4.3 全部字号）、`borderRadius`（§5.1）、`boxShadow`（§5.2）、`spacing`（§6）。
6. 建立 §1 的完整目录结构（空文件占位即可）。

**涉及文件**：`package.json`、`vite.config.ts`、`tailwind.config.js`、`tsconfig.json`、`index.html`、`src/main.tsx`、`src/App.tsx`、`src/styles/tokens.css`、`src/styles/global.css` 及目录骨架。

**完成标准（如何判断做完）**
- [ ] `npm run dev` 启动无报错，页面正常渲染。
- [ ] 页面背景为 `#F7F2EA`、主文字为 `#3A2C1F`（肉眼核对，或 DevTools 查看 computed style）。
- [ ] 用 Tailwind 类 `bg-brown text-ink`（或引用变量的类）能输出正确颜色，证明 Token 映射生效。
- [ ] `npm run build` 通过，无 TS 报错。

---

### Step 1 · 数据模型 + 状态管理 + 本地持久化

**依赖**：Step 0

**目标**：定义领域类型，建立 Zustand 全局状态与 LocalStorage 持久化，这是所有页面共用的地基。

**要实现的功能**
1. `src/types/index.ts` 定义类型，字段对齐 PRD §7：
   - `Guide { id, title, coverImage, destination, startDate, endDate, companions[], status, days: Day[], totalBudget?, createdAt }`
   - `Day { dayNumber, date, items: Item[] }`
   - `Item { id, type: 'transport'|'location'|'food'|'hotel', content, order, metadata, checked? }`（`metadata` 存车次/站名/备注等）
   - `Location { id, name, lat, lng, country, city, visitCount, firstVisitDate }`
   - `Expense { id, guideId, amount, currency, category, note, date, locationId? }`
2. `src/store/useStore.ts` 用 Zustand 建立 store，actions 至少包含：`addGuide / updateGuide / addDay / updateDay / addItem / updateItem / removeItem / reorderItem / toggleItemChecked / addExpense / removeExpense / upsertLocation`。
3. 用 `zustand/middleware` 的 `persist` 持久化到 LocalStorage，key 建议 `travel-footprint-v1`。
4. `src/lib/id.ts` 生成唯一 id（`crypto.randomUUID()` 或 `nanoid`）。
5. `src/lib/format.ts`：金额格式化（`¥8,420`）、日期/星期计算（`7月6日（周一）`）、百分比。

**涉及文件**：`src/types/index.ts`、`src/store/useStore.ts`、`src/lib/id.ts`、`src/lib/format.ts`

**完成标准**
- [ ] 类型定义通过 TS 编译，无 `any`。
- [ ] 在组件或控制台中调用 store 能完成 guide/day/item/expense 的增删改查。
- [ ] 刷新页面后数据仍在（验证 LocalStorage 持久化生效）。
- [ ] 清理 LocalStorage 后应用回到空初始态、不报错。

---

### Step 2 · 应用外壳（路由 + 手机框架 + 底部导航 + 暗色切换）

**依赖**：Step 1

**目标**：搭出 App 的骨架（手机框、顶栏、底部 Tab、路由、暗色切换），后续页面都填进这个壳里。

**要实现的功能**
1. `App.tsx` / `router.tsx`：路由 —— `/`（首页）、`/map`（地图）、`/bill`（账单）、`/guide/:id`（编辑页）。
2. `components/layout/` 四个组件：
   - `StatusBar`：40px 高、13px/600、灵动岛 104×24 圆角 99 `#000`（UI Spec §7.1）。
   - `AppBar`：品牌「旅行足迹」19px/800 + 前置 9px 日落橙圆点 + 右侧主题按钮。
   - `TabBar`：首页/地图/编辑/账单 4 个等宽 Tab，图标 21px + 文字 11px，激活态暖棕、非激活 `--ink-3`（§7.2）。
   - `FAB`：56px 圆形、底暖棕、`＋`28px、阴影 `e-3`，仅首页显示，点击进入编辑页。
3. 手机框架：桌面端 `.phone` 390×840 圆角 46 边框+投影居中；`@media(max-width:430px)` 全屏铺满（§响应式规则）。
4. 暗色切换：`body.dark` class 切换，偏好可持久化（LocalStorage）。
5. 页面切换动画：Framer Motion `opacity + translateY(8px)` 0.4s ease（§8）。

**涉及文件**：`src/App.tsx`、`src/router.tsx`、`src/components/layout/*`

**完成标准**
- [ ] 四个 Tab 可切换、高亮正确、对应占位页显示。
- [ ] 桌面看到手机框、手机/模拟器上全屏。
- [ ] 暗色切换生效，背景与文字变为 §3.5 暗色值，刷新后保持。
- [ ] FAB 仅在首页出现，点击跳转编辑页。

---

### Step 3 · 首页（KPI + 攻略列表 + 空状态 + 新建攻略）

**依赖**：Step 2

**目标**：完成第一个真实页面 —— 首页，包含数据概览、攻略卡片墙、空状态与新建入口。

**要实现的功能**
1. `KpiCard`（§7.4）：标签 11.5px `--ink-2`、数值 22px/800、hint 11px `--olive`；圆角 14、阴影 `e-1`。
2. 首页 KPI 三卡：点亮城市数 / 总花费 / 旅行天数 —— 从 store 聚合计算（`lib/locations.ts`、`lib/expense.ts`）。
3. `GuideCard`（§7.4）：封面 128px 渐变 + 半透明 tag、标题 16px、meta 12.5px、进度条 5px 日落橙。
4. `EmptyState`：无攻略时显示「你的下一段冒险，从这里开始」+ 插画（PRD §9 特别要求 2）。
5. `NewGuideModal`：点 FAB 弹出，填写标题/目的地/起止日期，保存后创建攻略。
6. 点击攻略卡片跳转 `/guide/:id`。

**涉及文件**：`src/pages/HomePage.tsx`、`src/components/guide/GuideCard.tsx`、`src/components/guide/NewGuideModal.tsx`、`src/components/ui/KpiCard.tsx`、`src/components/ui/EmptyState.tsx`

**完成标准**
- [ ] 无数据时显示温馨空状态（非空白）。
- [ ] 点 FAB 新建攻略后，卡片出现在列表，标题/日期正确。
- [ ] KPI 数字随攻略/账单数据变化（可手动造数据验证）。
- [ ] 点卡片能进入编辑页并显示该攻略标题。

---

### Step 4 · 攻略编辑页骨架（信息栏 + 天块时间轴）

**依赖**：Step 3

**目标**：完成编辑页主体骨架 —— 攻略信息栏 + 可折叠的「天」时间轴。

**要实现的功能**
1. `InfoBar`（§7.4）：封面 58×58 渐变、标题 16px、副标题 12px（目的地/日期）、同行人 tag 11px 底 `--ring`。
2. `DayBlock`（§7.5）：天块标题格式 `7月6日（周一）｜郑州 → 兰州`（日期+星期由 `lib/format.ts` 生成，路线可手动填写）；天块可折叠/展开。
3. 支持「添加天」「删除天」操作。
4. 折叠态样式：隐藏 `day-body`（§7.5 `.day.collapsed`）。

**涉及文件**：`src/pages/EditorPage.tsx`、`src/components/guide/DayBlock.tsx`、`src/components/ui/InfoBar.tsx`

**完成标准**
- [ ] 进入编辑页显示信息栏 + 该攻略的天块列表。
- [ ] 能新增/删除/折叠/展开天块，标题格式正确（含星期与路线）。
- [ ] 天块顺序可调整（基础版先支持上移/下移，拖拽见 Step 5）。

---

### Step 5 · 四类条目系统（添加 / 编辑 / 拖拽排序 / 打卡）

**依赖**：Step 4

**目标**：实现编辑器的核心 —— 交通/景点/美食/住宿四类条目，含底部滑动面板、拖拽排序与打卡。

**要实现的功能**
1. `ItemIcon`（§7.11）：30×30 圆角 9，底色按类型：交通暖棕 16%、景点日落橙 18%、美食橄榄绿 16%、住宿深蓝 13%；emoji 15px。
2. `Item`（§7.5）：图标 + 正文 14px（备注 12px `--ink-2`）+ 打卡圈（20px 圆形橄榄绿 `✓`）；景点自动序号 `①`（颜色 `--c-ticket`），当天内自动连续编号，增删后重排。
3. `BottomSheet`（§7.9）：底部滑动面板，含遮罩 `rgba(40,26,12,.4)`、手柄 38×4、四类选项网格，滑入动画 0.32s。
4. 拖拽排序：用 `@dnd-kit` 实现条目间与天块间的拖拽，放下后更新 `order` 并持久化。
5. 打卡：`toggleItemChecked` 随时切换（单一编辑态，无模式切换）。
6. `QuickAddBar`：天块底部的「＋ 添加条目」快捷条（虚线边框，§7.3）。

**涉及文件**：`src/components/guide/Item.tsx`、`ItemIcon.tsx`、`QuickAddBar.tsx`、`src/components/ui/BottomSheet.tsx`、`src/pages/EditorPage.tsx`

**完成标准**
- [ ] 四类条目都能通过底部面板添加，图标底色正确。
- [ ] 景点序号当天内自动连续，删除中间项后自动重排。
- [ ] 条目与天块拖拽排序生效且刷新后顺序保持。
- [ ] 打卡态可随时切换，样式为橄榄绿 `✓`。

---

### Step 6 · 地图页（点亮 + 统计 + 筛选 + 暗色星空）

**依赖**：Step 5（编辑器能产生地点数据）

**目标**：实现「旅行地图点亮」—— 攻略中的地点自动在地图上点亮并统计。

**要实现的功能**
1. `MapView`（§7.10）：用 `Leaflet + react-leaflet` 渲染地图，瓦片加暖色 CSS filter；已点亮地点用自定义 `divIcon`（日落橙 13px + 脉冲光环），未点亮灰点。
2. `MapStats`（§7.4 地图统计卡）：`3 国 / 6 城 / 12 地点`，由 `lib/locations.ts` 聚合 `Location` 数据。
3. `CityChips`（§7.6）：横向滑动城市筛选 chips，激活暖棕。
4. 点亮联动：编辑页添加「景点」条目时 `upsertLocation`（含 lat/lng，v1 可用预设城市坐标字典），地图随即点亮。
5. 暗色星空：`body.dark` 时地图背景变深蓝星空 + 金色光点（§7.10）。

**涉及文件**：`src/pages/MapPage.tsx`、`src/components/map/*`、`src/lib/locations.ts`

**完成标准**
- [ ] 编辑页添加的景点在地图上对应位置点亮（脉冲动画）。
- [ ] 统计面板数字与地点数据一致。
- [ ] 城市筛选 chips 能过滤显示。
- [ ] 暗色模式切换后地图变星空样式。

---

### Step 7 · 账单页（记账 + 分类图表 + 预算 + 明细筛选）

**依赖**：Step 1（expense 数据已可用）

**目标**：实现「旅行账单」—— 按攻略记账、分类统计、可视化图表与明细。

**要实现的功能**
1. 记账：在编辑页或账单页添加 `Expense`（金额、分类、备注、日期），分类用 6 类固定色（§3.3）。
2. `BillPage` 顶部三卡：总支出 / 日均 / 预算进度条（6px，`--olive→--orange` 渐变，§7.7）。
3. `CategoryDonut`（§7.8）：用 Recharts 环形图，颜色取类别色 Token；中心显示总支出；右侧图例（色块+名称+金额+百分比）。
4. `TrendChart`（§7.8）：Recharts 折线/面积图，描边 `#E88D4B`，面积渐变。
5. `BillRow` + `Filters`（§7.6/7.4）：明细列表 + 分类筛选 chips。
6. `lib/expense.ts`：按攻略/分类聚合、日均、人均、预算进度计算。

**涉及文件**：`src/pages/BillPage.tsx`、`src/components/bill/*`、`src/lib/expense.ts`

**完成标准**
- [ ] 添加支出后，总支出/日均/预算进度更新。
- [ ] 环形图与图例颜色与 §3.3 类别色一致，占比正确。
- [ ] 明细筛选生效；金额格式化 `¥8,420`。
- [ ] 图表有文字标注，不单独靠颜色区分（§10 无障碍）。

---

### Step 8 · 分享图生成

**依赖**：Step 5

**目标**：实现「随时生成分享图」—— 把攻略一键排版为可保存的长图/海报。

**要实现的功能**
1. `ShareImage`：隐藏渲染区，按分享排版（封面 + 标题 + 日期 + 时间轴摘要）组织攻略内容。
2. 用 `html-to-image` 将渲染区导出为 PNG（避免 canvas 跨域污染）。
3. 「生成分享图」主按钮（§7.3 主按钮）触发导出与下载/保存。
4. 遵循「自由编辑原则」：分享按钮全程可用，不受编辑状态限制。

**涉及文件**：`src/components/guide/ShareImage.tsx`、`src/lib/share.ts`、`src/pages/EditorPage.tsx`

**完成标准**
- [ ] 点击「生成分享图」能导出包含封面/标题/行程摘要的图片。
- [ ] 导出图片排版美观、内容完整、无报错。
- [ ] 按钮在编辑任意阶段均可点击。

---

### Step 9 · 收尾（首屏 Loading + 全站暗色 + 动效 + 无障碍 + 响应式）

**依赖**：Step 2–8 全部 UI

**目标**：补齐体验细节，达到可发布质量（对应 PRD §9 特别要求 + UI Spec §8/§9/§10）。

**要实现的功能**
1. `Loading`：首屏加载动画（飞机飞过 / 地图逐渐展开），≤2 秒（PRD §9.1）。
2. 全站暗色覆盖：图表、地图、所有面板在暗色下正确显示（§3.5/§9）。
3. Framer Motion 动效对齐 §8（页面切换、面板滑入、脉冲）。
4. 无障碍：`prefers-reduced-motion` 时关闭脉冲/位移动画；可点击目标 ≥44px；图表加 `aria-label`/图例（§10）。
5. 响应式与安全区：移动端 `env(safe-area-inset-bottom)`、真机/模拟器无横向溢出。
6. 全站自查：对照 UI 规范逐项核对颜色/字号/圆角/阴影与 demo 一致。

**涉及文件**：`src/components/ui/Loading.tsx` 及所有组件

**完成标准**
- [ ] 首屏出现 Loading 动画，随后进入首页。
- [ ] 暗色模式全站一致，无「漏暗」的区块。
- [ ] 系统「减弱动态效果」开启时动画被抑制。
- [ ] 手机/模拟器上无溢出，关键按钮可点。
- [ ] 对照 `demo.html` 走查，视觉一致。

---

### Step 10–12 · 后续版本（P2，非 v1 交付）

| Step | 功能 | 说明 |
|------|------|------|
| 10 | 多币种 + 汇率 + 从模板创建 | PRD §4 P2；账单支持多币种自动换算；新建攻略可选模板 |
| 11 | 云同步 | 接 Supabase/Firebase，多端同步（PRD §4 P2） |
| 12 | 小票 OCR + AI 辅助规划 | 拍照识别小票金额；输入目的地 AI 生成行程（PRD §4 P2） |

> 这三步依赖外部服务与后端，v1 不做；到达时另开计划细化。

---

## 3. 项目技术栈总览

### 3.1 框架与语言

| 项 | 选型 | 说明 |
|----|------|------|
| 构建工具 | **Vite 5** | 快速冷启动、开箱即用的 TS/React 支持 |
| 框架 | **React 18** | 组件化 UI |
| 语言 | **TypeScript 5** | 数据模型（PRD §7）已用 TS 定义，类型安全 |
| 路由 | **React Router 6** | 4 个页面 + 编辑详情路由 |

### 3.2 样式与动效

| 项 | 选型 | 说明 |
|----|------|------|
| 样式 | **Tailwind CSS 3.4** | 原子化布局；`tailwind.config` 映射 UI 规范 Token |
| 设计 Token | **CSS 变量（tokens.css）** | UI 规范 §2 的唯一配色来源 |
| 动效 | **Framer Motion** | 页面切换、面板滑入、列表布局动画（UI 规范 §8） |

### 3.3 状态与数据

| 项 | 选型 | 说明 |
|----|------|------|
| 状态管理 | **Zustand** | 轻量，`persist` 中间件直连 LocalStorage |
| 本地存储 | **LocalStorage** | 单用户本地版起步（PRD §8），后续换 Supabase/Firebase |
| 唯一 id | **nanoid**（或 `crypto.randomUUID()`） | 生成条目/攻略 id |

### 3.4 地图与图表

| 项 | 选型 | 说明 |
|----|------|------|
| 地图 | **Leaflet + react-leaflet** | 免费无 API Key，瓦片加暖色 CSS filter，自定义 `divIcon` 脉冲点亮 |
| 图表 | **Recharts** | 账单环形图 + 趋势折线图，颜色取类别色 Token |

### 3.5 交互与导出

| 项 | 选型 | 说明 |
|----|------|------|
| 拖拽排序 | **@dnd-kit** | 时间轴条目/天块拖拽 |
| 图片导出 | **html-to-image** | 分享图导出为 PNG（无 canvas 污染） |
| PWA（可选） | **vite-plugin-pwa** | 手机「添加到主屏幕」，本地优先体验 |

### 3.6 部署方式

| 项 | 说明 |
|----|------|
| 构建产物 | `npm run build` → 纯静态文件（无后端，数据在本地） |
| 托管 | **Vercel**（推荐）/ Netlify / GitHub Pages，任选其一 |
| 域名 | 可绑定自定义域名；PWA 需 HTTPS |
| 上线流程 | 提交到 Git 仓库 → 托管平台自动构建部署 |

> **选型理由**：本地优先、无后端、移动端体验为主的轻量 App，这套栈（Vite + React + Tailwind + Zustand + Leaflet + Recharts）全部免费、无 API Key 依赖，能最大化还原 demo 的视觉与动效，同时为后续接 Supabase 云同步留好接口。

---

*开发时以「完成标准」为验收依据；任何样式变更需同步更新《UI设计规范.md》与 `demo.html`。*
