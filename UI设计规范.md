# 旅行足迹 · UI 设计规范 v1.0

> **对应 demo**：`demo.html`（手机 App 模式）
> **适用范围**：旅行足迹 v1.0 前端开发
> **目的**：约束后续开发与 demo 样式完全一致，所有值可直接落地为 CSS 变量 / 设计 Token
> **更新日期**：2026-09-07

---

## 1. 设计原则

1. **温暖手账感**：大地色系（暖棕 / 米白 / 橄榄绿 / 日落橙），像翻阅一本旅行手账，而非冷冰冰的表单。
2. **移动端优先**：单列布局、底部 Tab 导航、拇指可达；桌面预览时以「手机框」呈现。
3. **单一编辑态**：不设「规划 / 记录 / 分享」模式切换，随时打卡、随时分享。
4. **颜色不单独承载含义**：图表/分类必须有图例 + 文字标注（见 §10 无障碍）。

---

## 2. Design Token 总览

以下 Token 以 CSS 自定义属性形式落地，**命名与 `demo.html` 完全一致**，开发时直接复制：

```css
:root {
  /* 品牌色 */
  --brown:#8B5E3C; --brown-d:#6E4930;   /* 暖棕（主）/ 暖棕深 */
  --cream:#F7F2EA;                       /* 米白（背景） */
  --olive:#6B7F5E;                       /* 橄榄绿（成功/完成） */
  --orange:#E88D4B;                      /* 日落橙（点亮/进度/强调） */
  --night:#0F1B33;                       /* 深蓝星空（暗色地图） */

  /* 中性色（文字/表面/边框） */
  --ink:#3A2C1F;        /* 主文字 */
  --ink-2:#8A7A68;      /* 次要文字 */
  --ink-3:#B4A893;      /* 弱化文字/占位 */
  --card:#FFFDF8;       /* 卡片/面板表面 */
  --line:#EADFCB;       /* 分割线/边框 */
  --ring:rgba(139,94,60,.14);   /* 高亮环形底色 */

  /* 类别色（账单分类 / 图标，固定顺序） */
  --c-transport:#8B5E3C;  /* 交通 · 暖棕 */
  --c-hotel:#6B7F5E;      /* 住宿 · 橄榄绿 */
  --c-food:#E88D4B;       /* 餐饮 · 日落橙 */
  --c-ticket:#C4643F;     /* 门票 · 陶土 */
  --c-shop:#C79A3B;       /* 购物 · 赭黄 */
  --c-other:#9C8E7C;      /* 其他 · 灰褐 */

  --radius:16px;
  --shadow:0 6px 20px rgba(90,66,40,.08);
}

body.dark {
  --cream:#161310; --card:#221C15;
  --ink:#F1E9DB; --ink-2:#A89B88; --ink-3:#6B6150;
  --line:#2E2720; --ring:rgba(232,141,75,.22);
  --shadow:0 8px 24px rgba(0,0,0,.45);
}
```

---

## 3. 配色方案

### 3.1 品牌色

| Token | 色值 | 用途 |
|-------|------|------|
| 暖棕 `--brown` | `#8B5E3C` | 主按钮、激活态、Tab 激活、主品牌 |
| 暖棕深 `--brown-d` | `#6E4930` | 主色按压/悬停加深 |
| 米白 `--cream` | `#F7F2EA` | 全局页面背景 |
| 橄榄绿 `--olive` | `#6B7F5E` | 成功/完成态（打卡✓）、正向 delta |
| 日落橙 `--orange` | `#E88D4B` | 点亮、进度条、强调、折线 |
| 深蓝星空 `--night` | `#0F1B33` | 暗色模式地图背景 |

### 3.2 中性色（墨色 / 表面 / 边框）

| Token | 色值 | 用途 |
|-------|------|------|
| `--ink` | `#3A2C1F` | 主文字、标题 |
| `--ink-2` | `#8A7A68` | 次要文字、副标题、meta |
| `--ink-3` | `#B4A893` | 弱化文字、占位、非激活图标 |
| `--card` | `#FFFDF8` | 卡片、面板、列表表面 |
| `--line` | `#EADFCB` | 分割线、边框、轨道 |
| `--ring` | `rgba(139,94,60,.14)` | 图标底色、高亮 wash、标签底 |

### 3.3 类别色（账单分类，固定顺序，不得循环/复用）

| 分类 | Token | 色值 | 占比示意 |
|------|-------|------|---------|
| 交通 | `--c-transport` | `#8B5E3C` | 38% |
| 住宿 | `--c-hotel` | `#6B7F5E` | 29% |
| 餐饮 | `--c-food` | `#E88D4B` | 18% |
| 门票 | `--c-ticket` | `#C4643F` | 9% |
| 购物 | `--c-shop` | `#C79A3B` | 4% |
| 其他 | `--c-other` | `#9C8E7C` | 2% |

> 规则：类别色**固定顺序、不随筛选变化**（颜色跟随实体，而非排名）；超过 6 类归入「其他」。所有类别必须搭配文字标注，不得只靠颜色区分。

### 3.4 状态色（固定，不用于普通系列）

| 状态 | 色值 | 说明 |
|------|------|------|
| 完成/成功 | `--olive #6B7F5E` | 打卡勾选、正向 |
| 进行中/强调 | `--orange #E88D4B` | 进度、点亮 |
| 危险/超支 | `#C0392B`（预留） | 未在 demo 出现，v1 预留 |

### 3.5 暗色模式

| Token | 亮色 | 暗色 |
|-------|------|------|
| 背景 `--cream` | `#F7F2EA` | `#161310` |
| 表面 `--card` | `#FFFDF8` | `#221C15` |
| 主文字 `--ink` | `#3A2C1F` | `#F1E9DB` |
| 次要文字 `--ink-2` | `#8A7A68` | `#A89B88` |
| 弱化 `--ink-3` | `#B4A893` | `#6B6150` |
| 边框 `--line` | `#EADFCB` | `#2E2720` |

暗色下地图改为**深蓝星空**：背景 `linear-gradient(#0F1B33, #0A1428)` + 白色星点，点亮地点为金色 `#FFD27A`（带光晕）。

---

## 4. 字体规范

### 4.1 字体族

```css
font-family: system-ui, -apple-system, "Segoe UI",
             "PingFang SC", "Microsoft YaHei", sans-serif;
```

- **不使用**衬线/展示字体，全站系统无衬线栈。
- 数字对齐：KPI、金额、百分比、轴刻度使用 `font-variant-numeric: tabular-nums`。

### 4.2 字重

| 名称 | 数值 | 用途 |
|------|------|------|
| Regular | 400 | 正文、说明 |
| Medium | 500 | 强调正文 |
| Semibold | 600 | 标题、按钮 |
| Bold | 700 | 标题、数值 |
| ExtraBold | 800 | 品牌、KPI 大数字 |

### 4.3 字号层级（Text Styles）

| Token | 字号 | 字重 | 行高 | 应用 |
|-------|------|------|------|------|
| `display` | 22px | 700 | 1.3 | 页面大标题（「我的旅行手账」） |
| `brand` | 19px | 800 | 1.2 | 顶栏品牌名 |
| `title` | 16px | 700 | 1.4 | 卡片标题、信息栏标题 |
| `heading` | 14px | 600 | 1.4 | 面板标题、日期块、按钮文字 |
| `subheading` | 13.5px | 600 | 1.4 | 图表卡标题 |
| `body` | 14px | 400 | 1.5 | 正文、时间轴条目 |
| `body-sm` | 13px | 400 | 1.5 | 副标题、主题按钮 |
| `caption` | 12.5px | 400 | 1.4 | 辅助说明、meta、图例、分类行 |
| `hint` | 11px | 400 | 1.3 | 弱化提示、标签、Tab 文字 |
| `micro` | 10.5px | 500 | 1.3 | 分类色块、小标注 |
| `number` | 22px | 800 | 1 | KPI 数值（tabular-nums） |
| `number-md` | 20px | 700 | 1 | 地图统计数值 |
| `number-sm` | 15px | 700 | 1 | 环形图中心数值 |

---

## 5. 圆角与阴影

### 5.1 圆角（Radius）

| Token | 值 | 用途 |
|-------|-----|------|
| `r-xs` | 6px | 色板小方块 |
| `r-sm` | 9px | 图标容器（`30×30`） |
| `r-md` | 12px | 信息栏封面 |
| `r-lg` | 14px | KPI 卡、主按钮、底部面板选项 |
| `r-xl` | 16px | 通用卡片、面板、图表卡、信息栏、day 块 |
| `r-2xl` | 18px | 攻略卡片、地图面板 |
| `r-sheet` | 22px（仅顶部） | 底部滑动面板 `22px 22px 0 0` |
| `r-full` | 999px | 胶囊标签、进度条、筛选 chip、分类徽章 |
| `r-circle` | 50% | FAB、打卡圈 |

### 5.2 阴影（Elevation）

| Token | 值 | 用途 |
|-------|-----|------|
| `e-1` | `0 6px 20px rgba(90,66,40,.08)` | 常规卡片/面板 |
| `e-2` | `0 8px 20px rgba(139,94,60,.3)` | 主按钮、分享按钮 |
| `e-3` | `0 12px 26px rgba(139,94,60,.42)` | FAB 悬浮按钮 |
| `e-sheet` | `0 -10px 30px rgba(60,42,20,.16)` | 底部滑动面板（向上） |
| `e-frame` | `0 30px 80px rgba(60,42,20,.28)` | 桌面手机框投影 |
| 暗色 | `0 8px 24px rgba(0,0,0,.45)` | 暗色模式统一阴影 |

---

## 6. 间距（4px 基础栅格）

| Token | 值 | 用途 |
|-------|-----|------|
| `space-1` | 4px | 图标与文字最小间隙 |
| `space-2` | 8px | 元素内紧凑间距 |
| `space-3` | 12px | 卡片间隙、条目上下内边距 |
| `space-4` | 16px | 屏幕左右边距、卡片标准内边距 |
| `space-5` | 20px | 区块间距、图表卡内边距 |
| `space-6` | 24px | 大区块分隔 |

- 屏幕内容区左右边距固定 **16px**；页面底部留白 **20px**。
- 卡片内边距：KPI 卡 `13–14px`，普通卡片 `16px`。

---

## 7. 组件规范

### 7.1 状态栏 / 顶栏

| 属性 | 值 |
|------|-----|
| 状态栏高度 | 40px |
| 状态栏文字 | 13px / 600 |
| 灵动岛 | 104×24px，圆角 99px，`#000` |
| 顶栏品牌 | 19px / 800，前置 9px 日落橙圆点（带 4px 光晕） |
| 主题切换按钮 | 圆角 999px，边框 `--line`，底 `--card`，文字 13px |

### 7.2 底部导航（Tab Bar）

| 属性 | 值 |
|------|-----|
| 结构 | 4 个等宽 Tab：首页 / 地图 / 编辑 / 账单 |
| 图标 | 21px emoji 图标 |
| 文字 | 11px；激活 600，非激活 400 |
| 激活色 | 暖棕 `--brown` |
| 非激活色 | 弱化 `--ink-3` |
| 容器 | 顶边框 `--line`，底 `--card`，底部含安全区 `env(safe-area-inset-bottom)` |

### 7.3 按钮

| 类型 | 规格 |
|------|------|
| **主按钮** | 底 `--brown`、白字 `#FFFDF8`、圆角 14px、高 48px、15px/600、阴影 `e-2` |
| **胶囊按钮**（主题切换等） | 边框 `--line`、底 `--card`、圆角 999px、13px |
| **FAB** | 56×56px 圆形、底 `--brown`、`＋` 28px、阴影 `e-3`，右下角距右 18px / 距底 76px |
| **筛选 Chip** | 12px、圆角 999px；激活底 `--brown` 白字，非激活边框 `--line` 灰字 |
| **底部面板选项** | 边框 `--line`、圆角 14px、图标 22px + 文字 13px、竖排 |
| **快捷插入**（虚线） | 边框 `1px dashed --ink-3`、圆角 10px、文字 12.5px |

### 7.4 卡片

| 卡片 | 圆角 | 内边距 | 说明 |
|------|------|--------|------|
| **KPI 统计卡** | 14px | 13–14px | 标签 11.5px `--ink-2`；数值 22px/800 `--ink`；hint 11px `--olive` |
| **攻略卡** | 18px | 封面 128px + 主体 14px | 封面渐变色 + 半透明 tag；标题 16px；meta 12.5px `--ink-2`；进度条见 7.7 |
| **图表卡** | 16px | 16px | 标题 13.5px/600 |
| **信息栏** | 16px | 14–15px | 封面 58×58 圆角 12px 渐变；标题 16px；副标题 12px；同行人 tag 11px |
| **通用面板/列表** | 16px | — | 底 `--card`、边框 `--line`、阴影 `e-1` |

### 7.5 时间轴条目（Item）

- 结构：`图标容器 + 正文 + 打卡圈`，间距 11px，条目间 `1px dashed --line` 分割。
- **图标容器**：30×30px，圆角 9px，底色按类型（见 7.11），emoji 15px。
- **正文**：14px，行高 1.5；备注 12px `--ink-2`。
- **景点序号**：`①` 自动编号，颜色 `--c-ticket`。
- **打卡圈**：20px 圆形，底 `--olive`，白 `✓` 11px。

### 7.6 标签 / 徽章 / 筛选

| 类型 | 规格 |
|------|------|
| **分类徽章**（账单） | 10.5px/500、白字、圆角 999px、底为类别色 |
| **筛选 Chip** | 12px、圆角 999px、激活底暖棕白字 |
| **地点/同行人 tag** | 11px、底 `--ring`、文字 `--brown` |
| **攻略封面 tag** | 11.5px、白字、半透明白底 `rgba(255,255,255,.24)` + 4px 模糊 |

### 7.7 进度条

- 高 **5px**（攻略卡）/ **6px**（预算），圆角 99px。
- 轨道：`--line`；填充：日落橙 `--orange`（预算条为 `--olive → --orange` 渐变）。

### 7.8 图表

| 图表 | 规格 |
|------|------|
| **分类环形图** | `conic-gradient` 按类别色占比绘制，中心 27% 挖空显示总数；128px（移动） |
| **图例** | 每项：10px 色块（圆角 3px）+ 名称 12.5px + 金额 `--ink-2` + 百分比 `--ink-3`，tabular-nums |
| **趋势折线** | 描边 2.5px、`#E88D4B`、圆角端点；下方面积渐变 `#E88D4B` 35%→0；网格线 `--line`；轴刻度 9.5px `--ink-3` |
| **规则** | 类别色固定顺序；必须有图例 + 直接标注，不得只靠颜色 |

### 7.9 底部滑动面板（Bottom Sheet）

| 属性 | 值 |
|------|-----|
| 遮罩 | `rgba(40,26,12,.4)`，覆盖全屏 |
| 面板 | 顶部圆角 22px，底 `--card`，阴影 `e-sheet` |
| 拖拽手柄 | 38×4px、圆角 99px、`--ink-3`、居中 |
| 标题 | 14px/600 |
| 选项网格 | 4 列，图标 22px + 文字 13px |
| 动效 | `translateY(105%)→0`，0.32s `cubic-bezier(.4,0,.2,1)` |

### 7.10 地图

- **已点亮**：日落橙圆点 13px，外圈 2px 脉冲光环（`pulse` 动画）。
- **未点亮**：弱化灰 `--ink-3` 圆点，无光环。
- **城市名**：11px，亮色 `--ink` / 暗色 `#CDD8F0`。
- **图例**：左下角小卡，11px `--ink-2`。
- **暗色**：深蓝星空背景 + 金色光点（`#FFD27A`）。

### 7.11 图标（条目类型底色）

| 类型 | emoji | 底色（在表面上叠加） |
|------|-------|---------------------|
| 交通 | 🚞🚇🚌🚗🚕🚶🏻‍♂️🚲 | 暖棕 16% `color-mix(#8B5E3C 16%)` |
| 景点 | 📍 | 日落橙 18% `color-mix(#E88D4B 18%)` |
| 美食 | 🍜 | 橄榄绿 16% `color-mix(#6B7F5E 16%)` |
| 住宿 | 🏨 | 深蓝 13% `color-mix(#0F1B33 13%)` |

---

## 8. 动效规范

| 场景 | 规格 |
|------|------|
| 页面切换 | `opacity 0→1` + `translateY(8px→0)`，**0.4s ease** |
| 底部面板 | 滑入 `0.32s cubic-bezier(.4,0,.2,1)` |
| 地图脉冲 | `scale .5→1.6` + `opacity .9→0`，**2.2s ease-out** 无限循环 |
| 通用过渡 | 0.2–0.35s ease（颜色/阴影/悬停） |
| 暗色切换 | 背景与文字过渡 **0.35s ease** |

> 遵循「减弱动态效果」系统偏好（`prefers-reduced-motion` 时关闭脉冲/位移动画）。

---

## 9. 暗色模式

- 通过 `body.dark` 切换，替换 §3.5 所列 Token。
- 地图单独变为深蓝星空（§7.10）。
- 类别色 / 品牌色在暗色下**保持原值**，仅中性色与表面变化。

---

## 10. 无障碍与数据可视化

1. **颜色不单独承载含义**：图表分类、打卡态、状态必须有图标 / 文字 / 图例辅助。
2. **文字对比度**：主文字对表面 ≥ 4.5:1；弱化文字仅用于辅助信息。
3. **触控目标**：可点击元素不小于 44×44px（FAB 56px、Tab 全宽、主按钮 48px）。
4. **图表**：环形图配完整图例与直接标注；折线图配 `role="img"` + `aria-label`。
5. **动效**：支持系统「减弱动态」偏好。

---

## 11. 落地对照（Demo → 代码）

| 规范项 | demo 中的实现位置 | 开发复用 |
|--------|------------------|---------|
| 全部 Token | `:root` / `body.dark` 变量块 | 直接复制 §2 |
| 字体族 | `body` | §4.1 |
| 页面骨架 | `.phone > .screen > .page` | 移动端单列 |
| 底部导航 | `.tabbar > .tab` | §7.2 |
| 卡片 | `.kpi / .guide-card / .chart-card / .info-bar` | §7.4 |
| 条目 | `.item / .ic / .check` | §7.5 |
| 底部面板 | `.bottom-sheet / .sheet-mask` | §7.9 |

---

*本规范由 `demo.html` 实测样式提炼，v1.0 开发以此为准；样式调整需同步更新本文件与 demo。*
